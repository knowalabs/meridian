---
description: Apply the refactor skill to restructure code without changing behavior.
argument-hint: [file, module or area to refactor]
---

Use the `refactor` skill — @.claude/skills/refactor/SKILL.md — and follow its
pin-then-transform-then-verify workflow end to end.

`$ARGUMENTS` names the file, module or area to refactor. Empty means ask
which area — refactor has no repo-wide default scope.
