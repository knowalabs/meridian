---
description: Apply the handle-errors skill to an error-handling or validation change.
argument-hint: [file or change to check]
---

Use the `handle-errors` skill — @.claude/skills/handle-errors/SKILL.md — and
follow it end to end for how this project fails and what its public surface
may expose.

`$ARGUMENTS` scopes the check to a file or path. Empty applies it to the
error-handling and validation code in the current diff.
