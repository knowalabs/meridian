An honest register of known debt in the Meridian CLI itself, for triage — not a judgment on the team, and not a duplicate of [engineering-standards.md](engineering-standards.md)'s standards framing. Read this when deciding what to fix next.

| Area                               | Description                                                                                                                                                                                                                    | Impact                                                                                                                                                | Suggested fix                                                                                                                                                           |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CI dependency scanning             | `.github/workflows/ci.yml`'s `test` job runs lint, format-check, build, coverage, and e2e — no `npm audit` or CodeQL step. `.github/dependabot.yml` exists in the repo but its configured scope wasn't visible in this review. | Medium — a vulnerable transitive dependency (this CLI stores API keys and writes files into other people's projects) would not be caught by CI today. | Add `npm audit --audit-level=high` to the `test` job, or a CodeQL workflow; confirm `.github/dependabot.yml`'s actual scope.                                            |
| `--estimate` pricing coverage      | `MODEL_PRICING` (`src/providers/router.ts`) has entries for exactly two models: `claude-sonnet-5` and `gemini-2.5-flash`. Every other provider/model combination reports "unknown cost" instead of a number.                   | Low/Medium — degrades gracefully (no wrong number is ever shown), but undercuts the feature's advertised value for most providers.                    | Expand the table for commonly-routed models, or document the coverage boundary explicitly in README.md.                                                                 |
| Single-maintainer bus factor       | `CODEOWNERS` routes every path, including the security-critical files, to one owner (`@sonal-sithara`); recent-activity data shows 1 contributor over 67 commits / 180 days.                                                   | High long-term — no second reviewer for the vault, the artifact write allowlist, or the publish workflow.                                             | Add a second CODEOWNER once available; until then, treat solo review of the flagged files as a known risk when reasoning about change safety.                           |
| Release-process fragility          | `CHANGELOG.md`'s `0.1.1` entry documents npm rewriting `bin.meridian` from `"./dist/index.js"` to `"dist/index.js"` during the `0.1.0` publish — a real, already-occurred incident, not hypothetical.                          | Medium — a future publish could silently diverge from the committed manifest again.                                                                   | Add a post-build, pre-publish verification step (packed-tarball diff or install smoke test) to the `publish` job in `.github/workflows/ci.yml`.                         |
| `meridian login` / Cloud Sync stub | `loginCommand` (`src/commands/update.ts`) is an explicit no-op that prints "not available yet."                                                                                                                                | Low — clearly labeled, not a hidden gap, but a real half-built command surface.                                                                       | No urgent fix; track its completion in [roadmap.md](roadmap.md) so it doesn't linger unlabeled past its stated scope.                                                   |
| Rapid, repeated rebranding         | The product renamed twice in quick succession (DevPilot → Knowa → Meridian) and republished under a new npm scope (`@knowalabs/meridian`), restarting the version at `0.1.0` (`CHANGELOG.md`, recent commit history).          | Low/Medium — any hardcoded product name, repo URL, or npm scope in generated output or docs risks going stale again.                                  | Treat product/package identity as unstable metadata in any future doc or template; avoid hardcoding it where a single source (e.g. `package.json`) can be read instead. |

## Related

- [engineering-standards.md](engineering-standards.md) — the same gaps framed against the stack's professional bar, with the standard each one falls short of.
- [roadmap.md](roadmap.md) — trajectory context for the rebranding and Cloud Sync entries above.
